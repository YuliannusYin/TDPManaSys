# 教师数字画像系统 - 项目开发计划书

## 1. 项目概述

**项目名称：** 教师数字画像系统（Teacher Digital Portrait System）

**项目目标：** 构建一个基于Spring Boot + Vue的全栈Web应用，实现高校教师科研成果的统一管理与可视化数字画像展示。

**核心价值：** 通过多维度成果数据的雷达图与统计图表，直观呈现教师综合能力，服务于个人发展自评与学院统筹管理。

**技术栈：**

- **后端：** Spring Boot 2.7.x + MyBatis-Plus + MySQL 8.0 + JWT + Swagger
- **前端：** Vue 3 + Element Plus + ECharts 5 + Axios
- **工具：** Maven（后端构建）、Vite（前端构建）、Git（版本控制）

***

## 2. 系统架构设计

### 2.1 分层架构

```
┌─────────────────────────────────────────────────────┐
│                    前端 (Vue 3)                       │
│  Views → Components → API Module → Axios Instance   │
└──────────────────────┬──────────────────────────────┘
                       │ HTTP/REST API (JWT 认证)
┌──────────────────────┴──────────────────────────────┐
│                后端 (Spring Boot)                     │
│  Controller → Service → Mapper → MyBatis-Plus       │
│  Interceptor (JWT) ← AOP (日志/权限)                 │
└──────────────────────┬──────────────────────────────┘
                       │ JDBC
┌──────────────────────┴──────────────────────────────┐
│                  MySQL 8.0 数据库                     │
│  10张核心表 + 索引优化                                │
└─────────────────────────────────────────────────────┘
```

### 2.2 项目目录结构

```
teacher-portrait/
├── portrait-server/                    # 后端项目
│   ├── src/main/java/com/portrait/
│   │   ├── config/                     # 配置类（Security、CORS、Swagger）
│   │   ├── controller/                 # 控制器层（按模块分包）
│   │   │   ├── AuthController
│   │   │   ├── UserController
│   │   │   ├── ProjectController
│   │   │   ├── PatentController
│   │   │   ├── SoftwareController
│   │   │   ├── PaperController
│   │   │   ├── CompetitionController
│   │   │   ├── PortraitController
│   │   │   ├── ExcelController
│   │   │   └── ScoreConfigController
│   │   ├── service/                    # 业务逻辑层
│   │   │   ├── impl/
│   │   │   └── ...
│   │   ├── mapper/                     # MyBatis-Plus Mapper
│   │   ├── entity/                     # 实体类
│   │   ├── dto/                        # 数据传输对象
│   │   ├── vo/                         # 视图对象（图表数据）
│   │   ├── common/                     # 公共类
│   │   │   ├── Result.java             # 统一响应体
│   │   │   ├── PageResult.java         # 分页响应
│   │   │   └── BusinessException.java  # 业务异常
│   │   └── interceptor/                # JWT拦截器
│   └── src/main/resources/
│       ├── application.yml
│       └── db/migration/               # SQL脚本
│
├── portrait-web/                       # 前端项目
│   ├── src/
│   │   ├── api/                        # API接口模块
│   │   ├── views/                      # 页面组件
│   │   │   ├── login/
│   │   │   ├── dashboard/
│   │   │   ├── project/
│   │   │   ├── patent/
│   │   │   ├── software/
│   │   │   ├── paper/
│   │   │   ├── competition/
│   │   │   ├── portrait/
│   │   │   └── admin/
│   │   ├── components/                 # 公共组件
│   │   ├── router/                     # 路由配置
│   │   ├── store/                      # Pinia状态管理
│   │   └── utils/                      # 工具函数
│   └── package.json
│
└── docs/                               # 项目文档
    ├── 需求规格说明书.md
    ├── 项目计划书.md
    └── 数据库设计文档.md
```

***

## 3. 数据库设计

### 3.1 表结构定义

按需求文档中实体分析，设计10张核心表，DDL脚本如下：

```sql
-- 1. 用户表
CREATE TABLE `user` (
  `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
  `work_no` VARCHAR(20) NOT NULL UNIQUE COMMENT '工号',
  `name` VARCHAR(50) NOT NULL COMMENT '姓名',
  `college` VARCHAR(100) COMMENT '学院',
  `role` ENUM('TEACHER','ADMIN') NOT NULL DEFAULT 'TEACHER' COMMENT '角色',
  `password` VARCHAR(255) NOT NULL COMMENT '密码(BCrypt加密)',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) COMMENT='用户表';

-- 2. 纵向项目表
CREATE TABLE `vertical_project` (
  `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
  `user_id` BIGINT NOT NULL COMMENT '教师ID',
  `name` VARCHAR(200) NOT NULL COMMENT '项目名称',
  `project_no` VARCHAR(100) UNIQUE COMMENT '项目编号',
  `level` ENUM('国家级','省部级','市厅级','校级') NOT NULL COMMENT '项目级别',
  `source_unit` VARCHAR(200) COMMENT '来源单位',
  `start_date` DATE NOT NULL COMMENT '立项时间',
  `planned_end_date` DATE COMMENT '计划完成时间',
  `funding` DECIMAL(12,2) COMMENT '项目经费(万元)',
  `role` ENUM('主持','参与') NOT NULL COMMENT '教师参与角色',
  `status` ENUM('在研','已结题','延期') DEFAULT '在研' COMMENT '项目状态',
  `remark` VARCHAR(500) COMMENT '备注',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_level` (`level`),
  INDEX `idx_start_date` (`start_date`)
) COMMENT='纵向项目表';

-- 3. 横向项目表
CREATE TABLE `horizontal_project` (
  `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
  `user_id` BIGINT NOT NULL COMMENT '教师ID',
  `name` VARCHAR(200) NOT NULL COMMENT '项目名称',
  `company_name` VARCHAR(200) NOT NULL COMMENT '合作企业名称',
  `contract_amount` DECIMAL(12,2) NOT NULL COMMENT '合同金额(万元)',
  `sign_date` DATE NOT NULL COMMENT '签订日期',
  `end_date` DATE COMMENT '完成日期',
  `role` ENUM('主持','参与') NOT NULL COMMENT '教师参与角色',
  `status` ENUM('在研','已结题') DEFAULT '在研' COMMENT '项目状态',
  `remark` VARCHAR(500) COMMENT '备注',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX `idx_user_id` (`user_id`),
  CHECK (`contract_amount` > 0)
) COMMENT='横向项目表';

-- 4. 专利表
CREATE TABLE `patent` (
  `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
  `user_id` BIGINT NOT NULL COMMENT '教师ID',
  `name` VARCHAR(200) NOT NULL COMMENT '专利名称',
  `type` ENUM('发明专利','实用新型','外观设计') NOT NULL COMMENT '专利类型',
  `application_no` VARCHAR(100) COMMENT '专利申请号',
  `grant_no` VARCHAR(100) COMMENT '专利授权号',
  `application_date` DATE NOT NULL COMMENT '申请日期',
  `grant_date` DATE COMMENT '授权日期',
  `status` ENUM('申请中','已授权') NOT NULL COMMENT '专利状态',
  `inventors` VARCHAR(500) COMMENT '发明人列表(分号分隔)',
  `patentee` VARCHAR(200) COMMENT '专利权人',
  `is_counted` TINYINT(1) DEFAULT 1 COMMENT '是否计入考核(1是0否)',
  `remark` VARCHAR(500) COMMENT '备注',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_type` (`type`),
  INDEX `idx_status` (`status`)
) COMMENT='专利表';

-- 5. 专利转让记录表
CREATE TABLE `patent_transfer` (
  `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
  `patent_id` BIGINT NOT NULL COMMENT '专利ID',
  `transfer_date` DATE NOT NULL COMMENT '转让日期',
  `transferee` VARCHAR(200) NOT NULL COMMENT '受让方名称',
  `amount` DECIMAL(12,2) COMMENT '转让金额(万元)',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX `idx_patent_id` (`patent_id`),
  FOREIGN KEY (`patent_id`) REFERENCES `patent`(`id`) ON DELETE CASCADE
) COMMENT='专利转让记录表';

-- 6. 软著表
CREATE TABLE `software_copyright` (
  `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
  `user_id` BIGINT NOT NULL COMMENT '教师ID',
  `name` VARCHAR(200) NOT NULL COMMENT '软件名称',
  `registration_no` VARCHAR(100) NOT NULL UNIQUE COMMENT '登记号',
  `version` VARCHAR(50) COMMENT '版本号',
  `dev_completion_date` DATE NOT NULL COMMENT '开发完成日期',
  `first_publish_date` DATE COMMENT '首次发表日期',
  `registration_date` DATE NOT NULL COMMENT '登记日期',
  `copyright_owners` VARCHAR(500) COMMENT '著作权人(分号分隔)',
  `remark` VARCHAR(500) COMMENT '备注',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_registration_date` (`registration_date`)
) COMMENT='软著表';

-- 7. 论文表
CREATE TABLE `paper` (
  `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
  `user_id` BIGINT NOT NULL COMMENT '教师ID',
  `title` VARCHAR(300) NOT NULL COMMENT '论文题目',
  `type` ENUM('期刊论文','会议论文') NOT NULL COMMENT '论文类型',
  `journal_name` VARCHAR(200) NOT NULL COMMENT '期刊/会议名称',
  `volume` VARCHAR(50) COMMENT '卷号',
  `issue` VARCHAR(50) COMMENT '期号',
  `pages` VARCHAR(50) COMMENT '页码',
  `publish_date` DATE NOT NULL COMMENT '发表时间',
  `authors` VARCHAR(500) COMMENT '作者列表(分号分隔)',
  `author_order` INT NOT NULL COMMENT '教师作者排序',
  `doi` VARCHAR(200) COMMENT 'DOI号',
  `remark` VARCHAR(500) COMMENT '备注',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_publish_date` (`publish_date`)
) COMMENT='论文表';

-- 8. 论文收录关联表
CREATE TABLE `paper_index` (
  `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
  `paper_id` BIGINT NOT NULL COMMENT '论文ID',
  `index_type` ENUM('SCI','SSCI','EI','CSCD','CSSCI','北大核心','普通期刊','其他') NOT NULL COMMENT '收录类型',
  INDEX `idx_paper_id` (`paper_id`),
  FOREIGN KEY (`paper_id`) REFERENCES `paper`(`id`) ON DELETE CASCADE
) COMMENT='论文收录关联表';

-- 9. 竞赛指导表
CREATE TABLE `competition` (
  `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
  `user_id` BIGINT NOT NULL COMMENT '教师ID',
  `name` VARCHAR(200) NOT NULL COMMENT '竞赛名称',
  `organizer` VARCHAR(200) COMMENT '主办单位',
  `competition_date` DATE NOT NULL COMMENT '参赛时间',
  `student_team` VARCHAR(500) COMMENT '学生团队(分号分隔)',
  `award_level` ENUM('国家级','省级','校级') NOT NULL COMMENT '获奖级别',
  `award_grade` ENUM('特等奖','一等奖','二等奖','三等奖','优秀奖') NOT NULL COMMENT '获奖等级',
  `guide_rank` INT DEFAULT 1 COMMENT '指导排名',
  `certificate_no` VARCHAR(100) COMMENT '获奖证书编号',
  `remark` VARCHAR(500) COMMENT '备注',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_award_level` (`award_level`),
  INDEX `idx_competition_date` (`competition_date`)
) COMMENT='竞赛指导表';

-- 10. 评分权重配置表
CREATE TABLE `score_config` (
  `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
  `dimension` ENUM('科研项目','专利成果','软件著作','学术论文','竞赛指导') NOT NULL UNIQUE COMMENT '维度名',
  `weight` DECIMAL(5,2) NOT NULL COMMENT '权重值(百分比)',
  `scoring_rules` JSON COMMENT '计分规则JSON',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) COMMENT='评分权重配置表';

-- 初始化默认权重
INSERT INTO `score_config` (`dimension`, `weight`, `scoring_rules`) VALUES
('科研项目', 20.00, '{"host_national":20,"host_provincial":15,"host_municipal":10,"host_school":5,"participate_factor":0.5,"horizontal_per_10w":2}'),
('专利成果', 20.00, '{"invention_auth":20,"utility_auth":10,"design_auth":5,"pending_factor":0.5,"transfer_extra":5}'),
('软件著作', 20.00, '{"per_item":10}'),
('学术论文', 20.00, '{"A":25,"B":15,"C":10,"D":5,"first_author_factor":1.0,"other_author_factor":0.5}'),
('竞赛指导', 20.00, '{"national_special":30,"national_first":25,"national_second":20,"national_third":15,"provincial_special":20,"provincial_first":15,"provincial_second":10,"provincial_third":8,"school_special":10,"school_first":8,"school_second":5,"school_third":3,"second_guide_factor":0.7}');
```

***

## 4. API接口设计

### 4.1 统一响应格式

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

### 4.2 接口清单

| 模块        | 方法              | 接口路径                                  | 说明             | 权限         |
| --------- | --------------- | ------------------------------------- | -------------- | ---------- |
| **认证**    | POST            | `/api/auth/login`                     | 用户登录           | 公开         |
| **用户**    | GET             | `/api/users`                          | 用户列表           | 管理员        |
| <br />    | GET             | `/api/users/{id}`                     | 用户详情           | 管理员        |
| <br />    | POST            | `/api/users`                          | 新增用户           | 管理员        |
| <br />    | PUT             | `/api/users/{id}`                     | 编辑用户           | 管理员        |
| <br />    | DELETE          | `/api/users/{id}`                     | 删除用户           | 管理员        |
| **纵向项目**  | GET             | `/api/vertical-projects`              | 项目列表(分页+多条件查询) | 教师/管理员     |
| <br />    | GET             | `/api/vertical-projects/{id}`         | 项目详情           | 教师/管理员     |
| <br />    | POST            | `/api/vertical-projects`              | 新增项目           | 教师/管理员     |
| <br />    | PUT             | `/api/vertical-projects/{id}`         | 编辑项目           | 教师/管理员     |
| <br />    | DELETE          | `/api/vertical-projects/{id}`         | 删除项目           | 教师/管理员     |
| **横向项目**  | GET             | `/api/horizontal-projects`            | 项目列表           | 教师/管理员     |
| <br />    | POST/PUT/DELETE | `/api/horizontal-projects[/{id}]`     | CRUD           | 教师/管理员     |
| **专利**    | GET             | `/api/patents`                        | 专利列表           | 教师/管理员     |
| <br />    | POST/PUT/DELETE | `/api/patents[/{id}]`                 | CRUD           | 教师/管理员     |
| <br />    | POST            | `/api/patents/{id}/transfer`          | 专利转让           | 教师/管理员     |
| <br />    | GET             | `/api/patents/{id}/transfers`         | 转让记录           | 教师/管理员     |
| **软著**    | GET             | `/api/software-copyrights`            | 软著列表           | 教师/管理员     |
| <br />    | POST/PUT/DELETE | 同上                                    | CRUD           | 教师/管理员     |
| **论文**    | GET             | `/api/papers`                         | 论文列表           | 教师/管理员     |
| <br />    | POST/PUT/DELETE | 同上                                    | CRUD(含收录标签)    | 教师/管理员     |
| **竞赛**    | GET             | `/api/competitions`                   | 竞赛列表           | 教师/管理员     |
| <br />    | POST/PUT/DELETE | 同上                                    | CRUD           | 教师/管理员     |
| **数字画像**  | GET             | `/api/portrait/{userId}/radar`        | 雷达图数据          | 教师(本人)/管理员 |
| <br />    | GET             | `/api/portrait/{userId}/dashboard`    | 仪表盘汇总数据        | 教师(本人)/管理员 |
| <br />    | GET             | `/api/portrait/compare?userIds=1,2,3` | 多教师对比数据        | 管理员        |
| <br />    | GET             | `/api/portrait/{userId}/trend`        | 历年趋势数据         | 教师/管理员     |
| **权重配置**  | GET             | `/api/score-config`                   | 获取权重配置         | 管理员        |
| <br />    | PUT             | `/api/score-config`                   | 更新权重配置         | 管理员        |
| **Excel** | GET             | `/api/excel/template/{module}`        | 下载模板           | 管理员        |
| <br />    | POST            | `/api/excel/import/{module}`          | 批量导入           | 管理员        |
| <br />    | GET             | `/api/excel/export/{module}`          | 数据导出           | 教师(本人)/管理员 |

***

## 5. 前端路由与页面设计

### 5.1 路由规划

```javascript
const routes = [
  { path: '/login', component: LoginView, meta: { noAuth: true } },
  { path: '/', redirect: '/dashboard' },
  {
    path: '/dashboard',
    component: DashboardView,
    meta: { title: '首页仪表盘' }
  },
  {
    path: '/project',
    children: [
      { path: 'vertical', component: VerticalProjectView },
      { path: 'horizontal', component: HorizontalProjectView }
    ]
  },
  { path: '/patent', component: PatentView },
  { path: '/software', component: SoftwareView },
  { path: '/paper', component: PaperView },
  { path: '/competition', component: CompetitionView },
  { path: '/portrait/:userId?', component: PortraitView },
  {
    path: '/admin',
    meta: { role: 'ADMIN' },
    children: [
      { path: 'users', component: UserManageView },
      { path: 'score-config', component: ScoreConfigView },
      { path: 'import-export', component: ImportExportView }
    ]
  }
]
```

### 5.2 页面组件树

```
App.vue
├── LoginView.vue                    # 登录页
├── Layout.vue                       # 主布局(侧边栏+顶栏)
│   ├── DashboardView.vue            # 首页
│   │   ├── SummaryCards.vue         # 数据汇总卡片
│   │   └── QuickRadar.vue           # 快速雷达图入口
│   ├── VerticalProjectView.vue      # 纵向项目列表
│   │   ├── ProjectFormDialog.vue    # 项目表单弹窗
│   │   └── ProjectFilter.vue        # 查询过滤器
│   ├── HorizontalProjectView.vue    # 横向项目列表(复用组件)
│   ├── PatentView.vue               # 专利列表
│   │   ├── PatentFormDialog.vue
│   │   └── TransferDialog.vue       # 转让弹窗
│   ├── SoftwareView.vue             # 软著列表
│   ├── PaperView.vue                # 论文列表(收录标签多选)
│   ├── CompetitionView.vue          # 竞赛列表
│   ├── PortraitView.vue             # 数字画像页
│   │   ├── RadarChart.vue           # 雷达图(ECharts)
│   │   ├── CompareMode.vue          # 对比模式选择
│   │   ├── TrendChart.vue           # 趋势图(柱状图+折线)
│   │   └── DistributionChart.vue    # 分布图(饼图/环形图)
│   └── admin/
│       ├── UserManageView.vue       # 用户管理
│       ├── ScoreConfigView.vue      # 权重配置
│       └── ImportExportView.vue     # 导入导出
└── components/
    ├── PageTable.vue                # 通用表格组件
    ├── ConfirmDialog.vue            # 二次确认弹窗
    └── UploadExcel.vue              # Excel上传组件
```

***

## 6. 分步开发计划

### 阶段一：项目初始化与基础架构（1天）

**目标：** 搭建前后端项目骨架，实现登录认证与权限拦截

**任务清单：**

- [x] 初始化Spring Boot项目，配置MyBatis-Plus、Swagger、CORS
- [x] 初始化Vue 3项目，配置Element Plus、Axios、Vue Router、Pinia
- [x] 创建数据库及10张核心表，执行DDL脚本
- [x] 实现JWT登录接口（BCrypt密码校验）
- [x] 实现前端登录页与Token存储
- [x] 实现前端路由守卫（根据角色显示菜单）
- [x] 实现后端JWT拦截器 + 角色权限注解

**产出物：** 可运行的项目骨架，登录流程走通

***

### 阶段二：成果管理模块CRUD（2天）

**目标：** 完成五大成果模块的增删改查与查询功能

**任务清单：**

- [x] 纵向项目CRUD（含分页、多条件查询）
- [x] 横向项目CRUD（含合同金额校验）
- [x] 专利管理CRUD + 专利转让功能
- [x] 软著管理CRUD（登记号唯一校验）
- [x] 论文管理CRUD + 多选收录标签保存
- [x] 竞赛指导CRUD

**开发模式（每个模块重复）：**

1. 后端：Entity → Mapper → Service → Controller
2. 前端：API封装 → 列表页(表格+分页) → 表单弹窗 → 删除确认
3. 权限处理：Service层根据角色过滤数据范围

**业务规则要点：**

- 教师只能查看/操作本人数据
- 管理员可查看/操作全院数据
- 专利转让仅限"已授权"状态
- 同一项目编号不可重复

**产出物：** 五大模块可正常增删改查

***

### 阶段三：数字画像与可视化（1.5天）

**目标：** 实现雷达图、趋势图、分布图及多教师对比

**任务清单：**

- [ ] 后端：评分计算Service（根据配置规则计算各维度原始分）
- [ ] 后端：归一化算法（最高分100基准）
- [ ] 后端：雷达图数据接口 `/api/portrait/{userId}/radar`
- [ ] 后端：对比数据接口 `/api/portrait/compare`
- [ ] 后端：仪表盘汇总接口（经费、论文A/B类数、专利授权数等）
- [ ] 后端：历年趋势接口（按年度统计各模块数量/金额）
- [ ] 前端：ECharts雷达图组件（支持叠加对比模式）
- [ ] 前端：柱状图+折线图组件（Tab切换模块）
- [ ] 前端：饼图/环形图组件（级别/类型分布）
- [ ] 前端：数据汇总卡片组件
- [ ] 前端：对比模式（管理员可选2-5人）

**产出物：** 数字画像页可视化完整展示

***

### 阶段四：系统管理与配置（1天）

**目标：** 实现管理员专属功能

**任务清单：**

- [ ] 用户管理CRUD（管理员可增删改查教师账号）
- [ ] 评分权重配置页面（滑块调整百分比，总和=100%约束）
- [ ] 计分规则配置（可调整各级别分值）
- [ ] 配置保存后全局生效

**产出物：** 管理员系统配置功能完成

***

### 阶段五：Excel导入导出（1天）

**目标：** 实现Excel批量导入导出功能

**任务清单：**

- [ ] 后端：引入Apache POI / EasyExcel依赖
- [ ] 后端：模板下载接口（生成各模块标准模板）
- [ ] 后端：数据导入接口（校验+批量入库+错误返回）
- [ ] 后端：数据导出接口（按筛选条件导出）
- [ ] 前端：模板下载按钮
- [ ] 前端：上传组件 + 导入结果提示（成功/失败列表）
- [ ] 前端：导出按钮（带筛选条件）
- [ ] 导入日志记录与异常处理

**业务规则：**

- 重复数据自动跳过并提示
- 校验失败返回具体行号和错误原因

**产出物：** Excel导入导出功能完整可用

***

### 阶段六：测试与优化（1天）

**目标：** 全流程测试与性能优化

**任务清单：**

- [ ] 功能测试：按验收标准逐项验证
- [ ] 权限测试：验证教师间数据隔离、管理员全局访问
- [ ] 边界测试：空数据、异常输入、并发操作
- [ ] 性能测试：查询响应时间、图表加载时间
- [ ] SQL优化：检查索引、慢查询
- [ ] 前端优化：路由懒加载、图表按需引入
- [ ] Bug修复

**验收对照：**

1. ✅ 各模块数据可正常增删改查
2. ✅ 角色权限正确隔离
3. ✅ 雷达图数据与录入一致
4. ✅ 修改权重后雷达图同步变化
5. ✅ 多教师对比正确展示
6. ✅ Excel导入导出功能正常
7. ✅ 核心流程无Bug

***

## 7. 关键技术实现要点

### 7.1 权限控制实现

```java
// 后端：自定义注解 + AOP
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface RequireRole {
    String[] value() default {"TEACHER", "ADMIN"};
}

// Service层数据过滤
if (currentUser.getRole().equals("TEACHER")) {
    queryWrapper.eq("user_id", currentUser.getId());
}
```

### 7.2 评分计算引擎

```java
@Service
public class ScoreCalculationService {
    public Map<String, Double> calculateScores(Long userId) {
        ScoreConfig config = scoreConfigMapper.selectAll();
        Map<String, Double> rawScores = new HashMap<>();
        rawScores.put("科研项目", calcProjectScore(userId, config));
        rawScores.put("专利成果", calcPatentScore(userId, config));
        // ... 其他维度
        return normalize(rawScores);
    }
}
```

### 7.3 前端雷达图对比

```javascript
// ECharts雷达图配置
const option = {
  radar: {
    indicator: [
      { name: '科研项目', max: 100 },
      { name: '专利成果', max: 100 },
      // ...
    ]
  },
  series: [{
    type: 'radar',
    data: [
      { value: [80, 60, 70, 90, 50], name: '教师A' },
      { value: [70, 80, 60, 85, 65], name: '教师B' }
    ]
  }]
}
```

***

## 8. 开发规范

### 8.1 代码规范

- 后端遵循阿里巴巴Java开发规范
- 前端遵循Vue 3 Composition API风格
- 所有API使用RESTful风格
- 统一使用`Result<T>`包装响应

### 8.2 Git提交规范

```
feat: 新增XX功能
fix: 修复XX问题
refactor: 重构XX模块
docs: 更新文档
```

### 8.3 注释要求

- 所有Controller方法添加Swagger `@Operation`注解
- 复杂业务逻辑添加行内注释
- 数据库表添加COMMENT注释

***

## 9. 风险与注意事项

| 风险项               | 应对措施                        |
| ----------------- | --------------------------- |
| ECharts数据量过大导致渲染慢 | 使用数据聚合，图表按需加载               |
| Excel导入大数据量超时     | 限制单次导入上限500条，异步处理           |
| 评分规则频繁变更          | 规则JSON化存储，前端动态解析            |
| 浏览器兼容性问题          | 仅承诺支持最新版Chrome/Firefox/Edge |

***

**计划书版本：** V1.0\
**编制日期：** 2026-05-11\
**预计总工时：** 约7.5天
