# 教师数字画像系统 — 全面质量审计报告

**审计日期**：2026-05-14 | **审计范围**：`teacher-portrait/` 全部后端代码、前端代码、数据库脚本、配置文件、文档  
**审计标准**：阿里巴巴 Java 规范、Vue 3 最佳实践、MySQL 性能规范 | **审计严格度**：逐文件检查，无遗漏

---

---

## 一、后端架构与安全审计

### 1. Controller 层异常处理

| 检查项 | Controller | 状态 | 说明 |
|--------|-----------|:--:|------|
| try-catch 是否缺失 | AuthController | ✅ | 依赖 GlobalExceptionHandler，AuthService 内部抛 BusinessException |
| | UserController | ✅ | 手动抛 BusinessException 做角色检查 |
| | PortraitController | ✅ | `checkAccess()`/`getCurrentUserId()` 内部抛 BusinessException |
| | CompetitionController | ✅ | Service 层处理异常 |
| | PaperController | ✅ | 同上 |
| | PatentController | ✅ | 同上 |
| | SoftwareCopyrightController | ✅ | 同上 |
| | HorizontalProjectController | ✅ | 同上 |
| | VerticalProjectController | ✅ | 同上 |
| | ExcelController | ✅ | [L77-L114](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/controller/ExcelController.java#L77-L114) 有外层 try-catch + [L107-L110](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/controller/ExcelController.java#L107-L110) 逐行 catch |
| | ScoreConfigController | ✅ | 无异常抛出路径 |

---

### 2. 写操作 @Transactional 注解

| Service | 写方法 | @Transactional | 状态 |
|---------|--------|:--:|:--:|
| AuthService | `login()` — 只读 | N/A | ✅ |
| UserService | `create()` [L42](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/service/UserService.java#L42) | **缺失** | ❌ |
| UserService | `update()` [L59](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/service/UserService.java#L59) | **缺失** | ❌ |
| UserService | `delete()` [L77](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/service/UserService.java#L77) | **缺失** | ❌ |
| UserService | `resetPassword()` [L87](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/service/UserService.java#L87) | **缺失** | ❌ |
| CompetitionService | `create/update/delete` L80/95/113 | 有 | ✅ |
| PaperService | `create/update/delete` L116/130/151 | 有 | ✅ |
| PatentService | `create/update/delete/transfer` L87/103/122/135 | 有 | ✅ |
| SoftwareCopyrightService | `create/update/delete` L80/92/111 | 有 | ✅ |
| HorizontalProjectService | `create/update/delete` L87/101/122 | 有 | ✅ |
| VerticalProjectService | `create/update/delete` L87/106/134 | 有 | ✅ |
| ScoreCalculationService | 全部只读 | N/A | ✅ |

> **严重问题**：`UserService` 的 4 个写方法 (create/update/delete/resetPassword) 均缺少 `@Transactional(rollbackFor = Exception.class)`。当用户管理的 selectCount + insert 或 selectById + updateById 发生中间态异常时，可能产生脏数据。

---

### 3. N+1 查询

| 检查项 | 位置 | 状态 | 说明 |
|--------|------|:--:|------|
| Mapper XML 文件 | — | ✅ | 项目无 XML 映射文件 |
| PaperService.page() | [L65-L75](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/service/PaperService.java#L65-L75) | ✅ | 批量加载 paperIndexes + 用户 |
| CompetitionService.page() | [L48-L50](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/service/CompetitionService.java#L48-L50) | ✅ | 批量加载用户 |
| PatentService.page() | [L55-L57](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/service/PatentService.java#L55-L57) | ✅ | 批量加载用户 |
| ScoreCalculationService.loadAllData() | [L548-L598](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/service/ScoreCalculationService.java#L548-L598) | ⚠️ | 一次性全量加载所有表数据到内存，无缓存策略，数据量增长至万级以上存在 OOM 风险 |
| PortraitController.teachers() | [L94](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/controller/PortraitController.java#L94) | ⚠️ | `userMapper.selectList(null)` 无分页，全量返回 |
| ScoreCalculationService.calculateTrend() | [L239-L290](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/service/ScoreCalculationService.java#L239-L290) | ⚠️ | 5 年 × 6 维度 = 30 次独立查询，可优化为按年份范围一次查询再内存分组 |

---

### 4. JWT Secret 安全

| 检查项 | 位置 | 状态 | 说明 |
|--------|------|:--:|------|
| Secret 硬编码 | [application.yml: L44](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/resources/application.yml#L44) | ❌ | `teacher-portrait-secret-key-2026-very-long-secret-key-for-jwt` 明文硬编码，**应使用环境变量 `${JWT_SECRET}` 或外部密钥管理服务** |
| Secret 复杂度 | 同上 | ⚠️ | 包含可预测字符串 (`teacher-portrait`/`secret-key`/`2026`)。52 字符长度对 HS256 足够，但不够随机。建议 256 位随机密钥 |
| expiration 硬编码 | [L45](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/resources/application.yml#L45) | ⚠️ | 24 小时硬编码，未配置 refresh token |

---

### 5. CORS 配置

| 检查项 | 位置 | 状态 | 说明 |
|--------|------|:--:|------|
| `addAllowedOriginPattern("*")` + `setAllowCredentials(true)` | [CorsConfig.java: L15-L18](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/config/CorsConfig.java#L15-L18) | ❌ | **违反 CORS 安全规范**：`allowCredentials=true` 时不允许使用通配符 Origin。浏览器会拒绝此配置，且存在 CSRF 攻击风险。应将 `*` 替换为具体域名白名单 |

---

### 6. SQL 注入

| 检查项 | 状态 | 说明 |
|--------|:--:|------|
| `${}` 使用 | ✅ | 全局搜索零结果 |
| Mapper XML | ✅ | 无 XML 文件 |
| 查询方式 | ✅ | 全部使用 MyBatis-Plus LambdaQueryWrapper，底层 `#{}` 参数化 |

---

### 7. 未使用的 Import / 类

| 文件 | 行号 | 未使用的导入 | 状态 |
|------|------|------|:--:|
| [PortraitController.java](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/controller/PortraitController.java#L19) | L19 | `import java.util.Collections;` | ⚠️ |
| [ExcelController.java](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/controller/ExcelController.java#L21) | L21 | `import java.nio.charset.StandardCharsets;` | ⚠️ |
| [ScoreCalculationService.java](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/service/ScoreCalculationService.java#L4) | L4 | `import TypeReference;` | ⚠️ |
| [GlobalExceptionHandler.java](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/common/GlobalExceptionHandler.java#L3) | L3 | `import com.portrait.common.Result;` (同包冗余) | ⚠️ |
| ScoreCalculationService.java | L55 | `OBJECT_MAPPER` 声明但未使用 | ⚠️ |
| RoleAspect.java / RequireRole.java | — | `@RequireRole` 注解定义但**未被任何 Controller 使用**，角色检查全部手动实现 | ⚠️ |

---

### 8. Swagger/Knife4j API 文档

| 检查项 | 状态 | 说明 |
|--------|:--:|------|
| 所有 Controller 的 `@Tag` / `@Operation` | ✅ | 11 个 Controller 全部添加 |
| DTO/VO 的 `@Schema` 注解 | ⚠️ | **全部 DTO、VO、QueryDTO 缺少字段级别的 `@Schema(description="...")` 注解**，导致 Knife4j 文档中请求/响应参数描述不够详细 |

---

### 9. 密码处理

| 检查项 | 位置 | 状态 | 说明 |
|--------|------|:--:|------|
| 加密算法 | [AuthService.java: L31](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/service/AuthService.java#L31), [UserService.java: L50/L52/L90](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/service/UserService.java#L50) | ❌ | **使用 `DigestUtils.md5DigestAsHex()` MD5 哈希**，MD5 已被证明不安全（彩虹表攻击），必须迁移至 `BCryptPasswordEncoder` |
| 默认密码 | [UserService.java: L50](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/service/UserService.java#L50) | ⚠️ | 默认密码 `"123456"` 硬编码且过于简单 |
| 密码返回 | UserService L39/L55/L73 | ✅ | 返回前正确将 `password` 设为 `null` |
| 密码日志 | 全项目 | ✅ | 无密码日志打印 |

---

### 10. 全局异常处理器

| 已处理 | 状态 | 缺失 | 状态 |
|--------|:--:|------|:--:|
| `BusinessException` | ✅ [L12-L16](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/common/GlobalExceptionHandler.java#L12-L16) | `Exception` (受检异常兜底) | ⚠️ |
| `RuntimeException` | ✅ [L18-L22](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/common/GlobalExceptionHandler.java#L18-L22) | `MethodArgumentNotValidException` (@Valid 校验失败) | ⚠️ |
| | | `ConstraintViolationException` (方法参数校验失败) | ⚠️ |
| | | `HttpMessageNotReadableException` (JSON 解析失败) | ⚠️ |
| | | `BindException` (GET 请求参数校验) | ⚠️ |

> 当前缺失 `Exception` 兜底处理器 → `IOException`/`SQLException` 等受检异常将产生 Spring 默认 500 页面而非 JSON 响应。

---

---

## 二、权限与数据隔离审计

### 1. JWT 拦截器覆盖范围

| 检查项 | 位置 | 状态 | 说明 |
|--------|------|:--:|------|
| 拦截路径 | [WebMvcConfig.java: L18-L27](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/config/WebMvcConfig.java#L18-L27) | ✅ | `addPathPatterns("/api/**")`，排除 `/api/auth/login` + doc 路径 |
| OPTIONS 预检放行 | [JwtInterceptor.java: L20-L22](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/interceptor/JwtInterceptor.java#L20-L22) | ✅ | 正确放行 OPTIONS 请求 |
| Token 缺失处理 | [L24-L29](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/interceptor/JwtInterceptor.java#L24-L29) | ✅ | 返回 401 JSON |
| Bearer 前缀剥离 | [L32-L34](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/interceptor/JwtInterceptor.java#L32-L34) | ✅ | 正确剥离 "Bearer " 前缀 |
| 属性设置 | [L37-L41](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/interceptor/JwtInterceptor.java#L37-L41) | ✅ | 设置 userId, workNo, role, name |
| 解析失败处理 | [L43-L48](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/interceptor/JwtInterceptor.java#L43-L48) | ✅ | 记录日志 + 返回 401 |

---

### 2. update/delete 所有权校验

| Controller | 所有权校验方式 | 状态 |
|-----------|------------|:--:|
| PortraitController | `checkAccess(userId)` — TEACHER 不可查看他人 | ✅ |
| CompetitionController | Service 层 `getUserId()` 校验：`create()/update()/delete()` 均设置 `entity.setUserId()` 后通过 `getCurrentUserId()` 校验 | ✅ |
| PaperController | 同上模式 | ✅ |
| PatentController | 同上 | ✅ |
| SoftwareCopyrightController | 同上 | ✅ |
| HorizontalProjectController | 同上 | ✅ |
| VerticalProjectController | 同上 | ✅ |

---

### 3. getById 所有权校验

| 检查项 | 状态 | 说明 |
|--------|:--:|------|
| 前端编辑流程 | ⚠️ | 所有 `getXxx(id)` API 函数**未被任何组件调用**。编辑通过父组件传递整行数据 `{ ...row }`，从未触发按 ID 回查 GET 请求。虽然在当前业务流程中可接受，但若未来增加"直接通过 URL 进入详情页"功能则需要补充所有权校验 |

---

### 4. 管理员专属接口角色校验

| 接口 | 校验方式 | 位置 | 状态 |
|------|---------|------|:--:|
| `/api/portrait/dashboard` (全汇总) | `getCurrentUserRole()` 判断 ADMIN | [PortraitController.java: L54](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/controller/PortraitController.java#L54) | ✅ |
| `/api/portrait/compare` (对比) | `getCurrentUserRole()` 判断 ADMIN | [PortraitController.java: L77](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/controller/PortraitController.java#L77) | ✅ |
| `/api/users/**` (用户管理) | `@RequireRole` 未使用，手动 `getCurrentUserRole()` | UserController | ⚠️ 应使用 `@RequireRole("ADMIN")` |
| `/api/score-config/**` | 同上 | ScoreConfigController | ⚠️ |
| `/api/excel/**` | 同上 | ExcelController | ⚠️ |

> **建议**：管理员 Controller 应统一使用 `@RequireRole("ADMIN")` 替代手动 `getCurrentUserRole()` 判断。

---

### 5. 前端路由守卫

| 检查项 | 位置 | 状态 | 说明 |
|--------|------|:--:|------|
| `noAuth` meta 放行登录页 | [router/index.js: L95-L98](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-web/src/router/index.js#L95-L98) | ✅ | 登录页免认证 |
| Token 存在性校验 | [L100-L103](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-web/src/router/index.js#L100-L103) | ✅ | 缺失则携带 `redirect` 参数跳转登录 |
| 角色权限校验 | [L106-L111](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-web/src/router/index.js#L106-L111) | ✅ | `meta.role === 'ADMIN'` 时从 localStorage 校验角色 |
| 角色不匹配提示 | [L109](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-web/src/router/index.js#L109) | ⚠️ | 直接重定向 `/dashboard`，缺少 `ElMessage.warning('权限不足')` |

---

---

## 三、数据库与性能审计

### 1. AUTO_INCREMENT 主键

| 表 | 状态 | 表 | 状态 |
|---|:--:|---|:--:|
| `user` | ✅ | `paper` | ✅ |
| `vertical_project` | ✅ | `paper_index` | ✅ |
| `horizontal_project` | ✅ | `competition` | ✅ |
| `patent` | ✅ | `score_config` | ✅ |
| `patent_transfer` | ✅ | — | — |
| `software_copyright` | ✅ | **10 张表全部使用 AUTO_INCREMENT** | ✅ |

---

### 2. 高频查询字段索引

| 表 | 已有索引 | 缺失索引 | 状态 |
|---|------|---------|:--:|
| `user` | `work_no` UNIQUE | — | ✅ |
| `vertical_project` | `user_id`, `level`, `start_date` | `status`（在研/已结题筛选） | ⚠️ |
| `horizontal_project` | `user_id` | `status`, `sign_date`（日期范围） | ⚠️ |
| `patent` | `user_id`, `type`, `status` | `application_date` | ⚠️ |
| `patent_transfer` | `patent_id` | `transfer_date` | ⚠️ |
| `paper_index` | `paper_id` | `index_type`（收录类型筛选） | ⚠️ |
| 其余表 | 均覆盖 | — | ✅ |

---

### 3. SELECT * 查询

| 检查项 | 状态 | 说明 |
|--------|:--:|------|
| MyBatis-Plus selectList/selectPage | ⚠️ | 使用 `BaseMapper.selectPage()` 默认 `SELECT *`。对于列表查询可接受（VO 需要全字段），但 `ScoreCalculationService.loadAllData()` 中 `selectList(null)` 全量加载时若字段非常多（如 `remark`、`authors` 等长文本），可优化为只加载计分必要字段 |

---

### 4. 分页查询

| 检查项 | 状态 | 说明 |
|--------|:--:|------|
| MyBatis-Plus Page 对象 | ✅ | 全部 Service 的分页查询均使用 `new Page<>(pageNum, pageSize)` |
| 内存分页 | ✅ | 未发现内存分页代码 |
| MybatisPlusConfig 分页插件 | ✅ | `PaginationInnerInterceptor` 已配置 |

---

### 5. 全表扫描风险

| 问题 | 位置 | 严重度 |
|------|------|:--:|
| `selectList(null)` 全量加载 | ScoreCalculationService L548-L598 | ⚠️ 当前数据量小可接受，万级需优化 |
| `userMapper.selectList(null)` 无分页 | PortraitController L94 | ⚠️ |
| 6 张业务表缺少 `user_id` 外键约束 | V1__init.sql 多处 | ❌ 数据库层无参照完整性保障 |

---

### 6. 数据库连接池配置

| 检查项 | 位置 | 状态 | 说明 |
|--------|------|:--:|------|
| HikariCP 显式参数 | [application.yml](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/resources/application.yml) | ⚠️ | **未显式配置** `maximum-pool-size`、`connection-timeout`、`idle-timeout`、`minimum-idle`。HikariCP 默认 `maximumPoolSize=10`，高并发场景可能不足 |
| `useSSL=false` | [L12](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/resources/application.yml#L12) | ⚠️ | 生产环境禁用 SSL 存在中间人攻击风险 |
| SQL 日志 stdout | [L22](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/resources/application.yml#L22) | ⚠️ | `StdOutImpl` 生产环境应关闭（泄露 SQL + 影响性能） |
| `logic-delete-field: deleted` | [L25-L28](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/resources/application.yml#L25-L28) | ⚠️ | **全局配置了逻辑删除但 10 张表中均无 `deleted` 字段** — 误配置可能影响 MyBatis-Plus 行为 |
| root 密码硬编码 | L13-L14 | ❌ |

---

---

## 四、前端质量审计

### 1. 未使用的组件/API 封装/工具函数

| 文件 | 未使用函数 | 状态 |
|------|---------|:--:|
| `api/competition.js` | `getCompetition(id)` | ⚠️ |
| `api/horizontalProject.js` | `getHorizontalProject(id)` | ⚠️ |
| `api/paper.js` | `getPaper(id)` | ⚠️ |
| `api/patent.js` | `getPatent(id)`, `getPatentTransfers(id)` | ⚠️ |
| `api/softwareCopyright.js` | `getSoftwareCopyright(id)` | ⚠️ |
| `api/verticalProject.js` | `getVerticalProject(id)` | ⚠️ |
| `api/portrait.js` | `getPortraitDistribution(userId)` | ⚠️ |

> 原因：编辑时父组件将整行数据 `{ ...row }` 传递给 editData prop，从未按 ID 回查。

---

### 2. API 请求封装

| 检查项 | 状态 | 说明 |
|--------|:--:|------|
| 8 个 API 模块使用集中式 axios 实例 | ✅ | 全部 `import request from './request'` |
| 视图中绕过封装层 | ⚠️ | 4 处直接调 `request()`：UserManageView、ScoreConfigView、ImportExportView、DistributionChart |

---

### 3. 表单校验

| 文件 | 状态 | 说明 |
|------|:--:|------|
| LoginView | ✅ | `workNo`, `password` 校验 |
| HorizontalProjectFormDialog | ✅ | 6 字段完整校验 |
| PaperFormDialog | ✅ | 5 字段完整 |
| PatentFormDialog | ✅ | 4 字段完整 |
| TransferDialog | ✅ | 2 字段完整 |
| CompetitionFormDialog | ✅ | 4 字段完整 |
| SoftwareFormDialog | ✅ | 4 字段完整 |
| **ProjectFormDialog** | ⚠️ | 模板设 `prop="projectNo"` 但 `rules` 中无验证 |
| **UserManageView** | ⚠️ | "初始密码" 无 prop/rules |

---

### 4. 加载状态与空数据

| 检查项 | 状态 | 说明 |
|--------|:--:|------|
| 所有列表视图 v-loading | ✅ | 全覆盖 |
| 所有表单提交 :loading | ✅ | 全覆盖 |
| DashboardView 无 loading | ⚠️ | 6 张卡片加载前显示 `-` / `0.0` |
| PortraitView 无 loading | ⚠️ | 5 张汇总卡片 + 图表区域无 loading |
| ScoreConfigView 无 loading | ⚠️ | 权重配置加载无指示 |
| 空数据状态 | ⚠️ | **所有 View 均未使用 `<el-empty>`**，数据为空时仅显示空表格 |

---

### 5. ECharts dispose() 防止内存泄漏

| 组件 | dispose 调用 | 状态 |
|------|------------|:--:|
| RadarChart.vue | `onUnmounted(() => chartInstance.dispose())` L103 | ✅ |
| TrendChart.vue | `onUnmounted(() => chartInstance.dispose())` L72 | ✅ |
| DistributionChart.vue | `onUnmounted(() => chartInstance.dispose())` L70 | ✅ |

---

### 6. 路由懒加载

| 组件 | 加载方式 | 状态 |
|------|---------|:--:|
| Layout | `import Layout from '../views/Layout.vue'` **直接静态导入** | ⚠️ |
| 其余 12 个子路由 | 全部 `() => import(...)` | ✅ |

---

### 7. console.log 调试代码

| 搜索 | 结果 | 状态 |
|------|------|:--:|
| 全项目搜索 `console.log` | **零结果** | ✅ |

---

### 8. CSS 问题

| 问题 | 状态 |
|------|:--:|
| 7 个 View 中重复定义相同 scoped CSS（`.page-container`/`.page-header`/`.page-title`/`.search-card`/`.pagination-wrap`） | ⚠️ 应提取为全局样式 |
| LoginView.vue `.login-tips` 定义但对应 div 内容为空 | ⚠️ |

---

---

## 五、测试数据与文档审计

### 1. test-data.sql 数据完整性

| 维度 | 数量 | 状态 | 备注 |
|------|------|:--:|------|
| 用户 | 5 人 (T101-T105) | ✅ | |
| 纵向项目 | 44 条 | ✅ | |
| 横向项目 | 50 条 | ✅ | |
| 专利 | 34 条 | ✅ | |
| 软著 | 39 条 | ✅ | |
| 论文 | 49 条 | ✅ | |
| 竞赛 | 58 条 | ✅ | |
| 可重复执行 | SELECT INTO + 清理逻辑 | ✅ | |
| 密码 | MD5('123456') | ❌ | **同 V1__init.sql**，需迁移 BCrypt |
| 注释一致性 | 张三已授权数：注释写 2 实际 3 | ⚠️ | |

---

### 2. README.md 快速启动

| 检查项 | 状态 | 说明 |
|--------|:--:|------|
| Docker MySQL 启动 | ✅ | `docker compose up -d` 可执行 |
| SQL 导入步骤 | ⚠️ | `source V1__init.sql` 存在语法错误（见下方 C1） |
| test-data.sql 导入 | ✅ | 步骤清晰 |
| **默认账号表** | ❌ | README L86 列出 T002 教师账号，但 V1__init.sql 仅创建了 admin 和 T001，test-data.sql 创建的是 T101-T105 — **T002 不存在** |
| ECharts 版本号 | ⚠️ | README 标 5.5.0，package.json 为 `^6.0.0` |

---

### 3. DEVLOG.md

| 检查项 | 状态 | 说明 |
|--------|:--:|------|
| 开发历程完整性 | ✅ | 2026-05-11 ~ 2026-05-14，6 阶段 + 技术债务 + Bug 修复 |
| 版本号一致性 | ⚠️ | Node.js v24 vs README 要求 >=16 |
| 债务表 | ⚠️ | DEVLOG 仅列 1 项，README 列 13 项，两份文档不一致 |

---

### 4. docker-compose.yml

| 检查项 | 状态 | 说明 |
|--------|:--:|------|
| MySQL 服务 | ✅ | 端口 3308、字符集 utf8mb4、时区 Asia/Shanghai |
| **缺失服务** | ⚠️ | 仅有 MySQL，缺少 `portrait-server` 和 `portrait-web` 服务 — 无法 `docker compose up` 一键启动 |
| MySQL root 密码 | ⚠️ | `root` 弱密码 |
| 缺少 healthcheck | ⚠️ | 依赖方无法确认数据库就绪 |

---

### 5. 敏感信息泄露风险

| 文件 | 暴露内容 | 状态 |
|------|---------|:--:|
| application.yml | 数据库密码 `root` 明文 | ❌ |
| application.yml | JWT Secret 明文 | ❌ |
| docker-compose.yml | MySQL root 密码 | ⚠️ |
| .env 文件 | 不存在 | ✅ |

---

---

## 六、关键严重问题汇总 (CRITICAL)

| 编号 | 类别 | 位置 | 问题 |
|:--:|------|------|------|
| **C1** | 数据库 | [V1__init.sql: L190](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/resources/db/migration/V1__init.sql#L190) | **SQL 语法错误**：INSERT VALUES 最后一行末尾有逗号 `NULL),` → MySQL 执行报 `ERROR 1064` |
| **C2** | 密码安全 | [AuthService.java: L31](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/service/AuthService.java#L31), [UserService.java: L50/L52/L90](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/service/UserService.java#L50-L90), [V1__init.sql: L179/L183](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/resources/db/migration/V1__init.sql#L179) | **MD5 哈希不安全**，需全线迁移至 BCryptPasswordEncoder |
| **C3** | JWT 安全 | [application.yml: L44](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/resources/application.yml#L44) | JWT Secret 硬编码在配置文件中，需外部化至环境变量 |
| **C4** | CORS 安全 | [CorsConfig.java: L15-L18](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/java/com/portrait/config/CorsConfig.java#L15-L18) | `allowCredentials(true)` + `addAllowedOriginPattern("*")` 违反 CORS 安全规范 |
| **C5** | 依赖漏洞 | [pom.xml: L25](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/pom.xml#L25) | **jjwt 0.9.1** 存在已知 CVE (CVE-2022-25949 签名伪造)，需升级至 0.12.x |
| **C6** | 配置安全 | [application.yml: L13-L14](file:///B_ProjSourCodeLibrary/Projects/Project-Study-00/TDPManaSys/teacher-portrait/portrait-server/src/main/resources/application.yml#L13-L14) | 数据库 root 密码硬编码 |
| **C7** | 数据库 | V1__init.sql L25/46/64/98/116/146 | **6 张业务表缺少 `user_id` FOREIGN KEY 约束**，数据库层无参照完整性 |
| **C8** | 文档 | README.md L86 | 列出不存在的 T002 测试账号 |

---

---

## 七、修复优先级方案

### 🔴 P0 — 立即修复（阻断上线或安全漏洞）

| 优先级 | 编号 | 修复项 | 方案 |
|:--:|:--:|------|------|
| 1 | C1 | V1__init.sql 尾随逗号 | 删除 L190 `,` → `NULL)` |
| 2 | C5 | jjwt 0.9.1 漏洞 | 升级至 `jjwt.version=0.12.6`，修改 JwtUtil API（0.12.x 使用 `Jwts.parser().verifyWith(SecretKey)` 新 API） |
| 3 | C2 | MD5 → BCrypt | 引入 `spring-boot-starter-security` 的 `BCryptPasswordEncoder`，替换 AuthService/UserService/V1__init.sql/test-data.sql 中所有 MD5 调用 |
| 4 | C3 | JWT Secret 硬编码 | `application.yml` 改为 `${JWT_SECRET:default-dev-only}`，生产通过环境变量注入 |
| 5 | C4 | CORS 通配符 | 将 `addAllowedOriginPattern("*")` 改为 `addAllowedOrigin("http://localhost:3000")` |

### 🟡 P1 — 尽快修复（完整性/数据安全）

| 优先级 | 编号 | 修复项 | 方案 |
|:--:|:--:|------|------|
| 6 | C7 | 外键约束 | V1__init.sql 为 6 张表的 `user_id` 添加 `FOREIGN KEY REFERENCES user(id)` |
| 7 | C6 | 数据库密码 | 改为 `${DB_PASSWORD}` 环境变量 |
| 8 | C8 | README T002 | 移除或改为 T101（test-data.sql 中存在的用户） |
| 9 | — | UserService @Transactional | 4 个写方法添加 `@Transactional(rollbackFor = Exception.class)` |
| 10 | — | GlobalExceptionHandler 补充 | 添加 `Exception`/`MethodArgumentNotValidException`/`HttpMessageNotReadableException` 处理器 |

### 🟢 P2 — 计划修复（优化建议/技术债务）

| 优先级 | 编号 | 修复项 | 方案 |
|:--:|:--:|------|------|
| 11 | — | 缺失索引 | 补充 `status`/`sign_date`/`index_type`/`application_date` 索引 |
| 12 | — | HikariCP 显式配置 | 设置 `maximumPoolSize=20`, `connectionTimeout=30000`, `idleTimeout=600000` |
| 13 | — | DTO/VO @Schema 注解 | 为所有 DTO/VO 字段添加 `@Schema(description="...")` |
| 14 | — | 前端 v-loading 补充 | DashboardView/PortraitView/ScoreConfigView 添加加载状态 |
| 15 | — | `<el-empty>` 空状态 | 所有列表 View 的 tableData 为空时显示 `<el-empty>` |
| 16 | — | 管理员 @RequireRole | 为 UserController/ScoreConfigController/ExcelController 添加 `@RequireRole("ADMIN")` |
| 17 | — | CSS 提取 | 7 个 View 重复 CSS 提取为全局/公共样式 |
| 18 | — | docker-compose 完善 | 添加 portrait-server + portrait-web 服务、MySQL healthcheck |
| 19 | — | Flyway 依赖 | pom.xml 引入 Flyway，使 V1__init.sql 自动执行 |
| 20 | — | 移除 logic-delete-field | application.yml 删掉 `logic-delete-field: deleted` 配置（表中无此字段） |
| 21 | — | SQL stdout 关闭 | `log-impl` 改为 none 或仅 dev profile |
| 22 | — | 未使用代码清理 | 4 处 unused import + `OBJECT_MAPPER` + `@RequireRole` 激活或移除 |

---

---

## 八、审计总结表

| 类别 | 通过 | 警告 | 严重 | 检查项总数 |
|------|:--:|:--:|:--:|:--:|
| **后端架构与安全** | 4 | 9 | 3 | 16 |
| **权限与数据隔离** | 4 | 3 | 0 | 7 |
| **数据库与性能** | 2 | 8 | 3 | 13 |
| **前端质量** | 5 | 8 | 0 | 13 |
| **测试数据与文档** | 3 | 5 | 3 | 11 |
| **总计** | **18** | **33** | **9** | **60** |

### 总体评价

| 维度 | 评分 | 说明 |
|------|:--:|------|
| **功能完整性** | ⭐⭐⭐⭐⭐ | 42 个 REST API 全部可用，前后端功能完整 |
| **架构设计** | ⭐⭐⭐⭐ | 分层清晰，统一异常处理，MyBatis-Plus 使用规范 |
| **代码规范** | ⭐⭐⭐⭐ | 命名规范，结构清晰，controller 命名一致 |
| **安全性** | ⭐⭐ | MD5 密码 + jjwt 漏洞 + JWT/CORS 配置不安全 — **需优先修复** |
| **性能** | ⭐⭐⭐ | DataLoader 模式解决了 N+1，但全量加载无缓存策略 |
| **可维护性** | ⭐⭐⭐ | 缺少 Flyway，DTO/VO 缺少 @Schema，文档间不一致 |

---

> **结论**：项目功能实现完整、架构设计合理，主要问题集中在**安全配置**（MD5、jjwt漏洞、JWT硬编码、CORS通配符）和**数据库完整性**（外键缺失、SQL语法错误）。**9 项严重问题需在正式部署前全部修复**，33 项警告建议按 P0→P1→P2 优先级逐批处理。